INCLUDE(WebKitNetflix)

LIST(APPEND WebKit_SOURCES
    netflix/ChromeClientNetflix.cpp
    netflix/EditorClientNetflix.cpp
    netflix/FrameLoaderClientNetflix.cpp
    netflix/InspectorClientNetflix.cpp
    netflix/NPCompat.cpp
    netflix/WebViewNetflix.cpp
)

LIST(APPEND WebKit_INCLUDE_DIRECTORIES
    "${JAVASCRIPTCORE_DIR}/wtf"
    "${WEBKIT_DIR}/netflix/WebCoreSupport"
    "${WEBCORE_DIR}/platform/netflix"
    "${WEBCORE_DIR}/platform/image-decoders"
    "${WEBCORE_DIR}/platform/graphics/cairo"
    ${LIBXML2_INCLUDE_DIR}
    ${LIBXSLT_INCLUDE_DIRS}
    ${SQLITE_INCLUDE_DIRS}
)

IF (WTF_USE_FREETYPE)
  LIST(APPEND WebKit_INCLUDE_DIRECTORIES
    "${WEBCORE_DIR}/platform/graphics/freetype"
  )
  LIST(APPEND WebKit_LIBRARIES
     ${Freetype_LIBRARIES}
  )
ENDIF ()

IF (WTF_USE_CAIRO)
  LIST(APPEND WebKit_INCLUDE_DIRECTORIES
    ${Cairo_INCLUDE_DIRS}
  )
  LIST(APPEND WebKit_LIBRARIES
    ${Cairo_LIBRARIES}
  )
ENDIF ()

LIST(APPEND WebKit_LIBRARIES
    ${LIBXML2_LIBRARIES}
    ${SQLITE_LIBRARIES}
    ${PNG_LIBRARY}
    ${JPEG_LIBRARY}
    ${CMAKE_DL_LIBS}
)

IF (WTF_USE_CURL)
  LIST(APPEND WebKit_INCLUDE_DIRECTORIES ${CURL_INCLUDE_DIRS} ${ZLIB_INCLUDE_DIRS})
  LIST(APPEND WebKit_LIBRARIES ${CURL_LIBRARIES} ${ZLIB_LIBRARIES})
ENDIF ()

